<div class="container text-left mt-4">
  <%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>

  <%= f.input :address %>

  <%= f.input :category, collection: Restaurant::CUISINE, input_html: { multiple: false } %>

  <%= f.submit "Create" %>
  <% end %>
</div>
